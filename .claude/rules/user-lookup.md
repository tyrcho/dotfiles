# User Lookup & Identity Resolution

For Atlassian (Jira/Confluence) user lookups, always use account ID or email-based exact matching rather than display name matching. Handle accented characters (French names) with proper Unicode normalization.
